mutation contactUs(
    $name : String!,
    $email: String!,
    $telephone: String,
    $comment: String!
) {
    contactUs(
        name : $name,
        email: $email,
        telephone:  $telephone,
        comment: $comment
    )
}